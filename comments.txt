Привет, Егор!
Рада снова с тобой взаимодействовать!

Спасибо большое за ревью и за советы.

Загрузила индексы в оперативку с использованием SortedDict. Так действительно удобнее.

Не знала, что у винды и у линукса используются пробелы разных размеров в байтах. Спасибо большое, теперь буду знать :)
Добавила такой код, который автоматически определяет операционную систему, на которой этот код запускается. Это верно?
    # size of whitespace (depends on operating system)
    self.__ws_size = 2 if os.name == 'nt' else 1

Также вынесла добавление строки в файл индексов в отдельную функцию.